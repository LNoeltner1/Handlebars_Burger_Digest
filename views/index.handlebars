<h1>Burgers!</h1>

<h2>burger that have been eaten!!</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block sleep=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that haven't been eaten!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block sleep=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">devoured?</label><br>
        <input type="radio" name="devoured" value="1" checked> Not eaten!<br>
        <input type="radio" name="devoured" value="0"> Eaten!
    </div>

    <button type="submit">Add burger!</button>
</form>